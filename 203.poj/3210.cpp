#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("data.in", "r", stdin)
    int n;
    cin >> n;
    string a, b;
    while (n--)
    {
        cin.ignore();
        cin >> a;
        cin >> b;

    }
}